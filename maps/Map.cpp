//
// Created by Jeremi on 25.04.2024.
//
#include <SFML/Graphics.hpp>
#include "Map.h"
#include "string"

//bool Map::isIn(sf::Vector2f point){ // later to worldMap class
//    return (point.x<40 || point.x>1880) || (point.y<40 || point.y>1040) || (point.x>340 && point.x<560 && point.y<700) ||
//           (10000 > (point.x-1500)*(point.x-1500)+(point.y-800.)*(point.y-800.)) ||
//           (40000 > (point.x-1000)*(point.x-1000)+(point.y-500.)*(point.y-500.));
//}

//bool Map::isIn(sf::Vector2f point){
//    if(img.getPixel(point.x, point.y) == sf::Color::Black) {
//        return false;
//    }
//    else{
//        return false;
//    }
//}

//sf::Texture Map::getTexture() {return selfTxt;}